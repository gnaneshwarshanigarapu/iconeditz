import multer from 'multer';import { uploadToR2 } from './lib/r2.js';import { authorizeAdmin } from './lib/auth.js';import { withApi } from './lib/handler.js'
const upload=multer({storage:multer.memoryStorage(),limits:{fileSize:100*1024*1024}});export const config={api:{bodyParser:false}};export default withApi(['POST'],async(req,res)=>{authorizeAdmin(req);await new Promise((resolve,reject)=>upload.single('file')(req,res,error=>error?reject(error):resolve()));if(!req.file)throw Object.assign(new Error('File is required'),{status:400});res.status(201).json({data:await uploadToR2(req.file,req.body.folder||'uploads')})})
