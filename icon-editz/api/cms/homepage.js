import { z } from 'zod';import { supabaseAdmin } from '../lib/supabaseAdmin.js';import { authorizeAdmin } from '../lib/auth.js';import { withApi } from '../lib/handler.js'
export default withApi(['GET','PUT'],async(req,res)=>{if(req.method==='GET'){const {data,error}=await supabaseAdmin.from('homepage_content').select('*');if(error)throw error;return res.json({data:data??[]})}authorizeAdmin(req);const body=z.object({section:z.string(),content:z.record(z.any())}).parse(req.body);const {data,error}=await supabaseAdmin.from('homepage_content').upsert({...body,updated_at:new Date().toISOString()}).select().single();if(error)throw error;res.json({data})})
