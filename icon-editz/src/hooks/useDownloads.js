import { useQuery } from '@tanstack/react-query'; import { api } from '../services/api'; import { queryKeys } from '../constants/queryKeys'; export const useDownloads = () => useQuery({ queryKey: queryKeys.downloads, queryFn: () => api.get('/api/downloads') })
