import { useQuery } from '@tanstack/react-query'; import { api } from '../services/api'; import { queryKeys } from '../constants/queryKeys'; export const useHireRequests = (filters = {}) => useQuery({ queryKey: queryKeys.hireRequests(filters), queryFn: async () => { const query = new URLSearchParams(Object.entries(filters).filter(([, value]) => value)); return (await api.get(`/api/hire-requests?${query}`)).data || [] } })
