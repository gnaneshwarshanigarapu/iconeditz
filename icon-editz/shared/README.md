# Shared contracts

Place versioned, runtime-neutral API contracts here as each FastAPI endpoint reaches parity. Browser code must use API clients and must not import backend secrets or Supabase service-role configuration.
